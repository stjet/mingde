import { Window } from '../wm.js';

//vim but worse

enum MalvimMessage {
  //
}

export class Malvim extends Window<MalvimMessage> {
  //
  constructor(size: [number, number]) {
    super(size, "Malvim", "malvim");
    //
  }
  //
}

