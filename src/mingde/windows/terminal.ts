import { VerticalScrollable } from '../vertical_scrollable.js';

//text input and scrollables will need to be implemented

enum TerminalMessage {
  //
}

export class Terminal extends VerticalScrollable<TerminalMessage> {
  //
}

